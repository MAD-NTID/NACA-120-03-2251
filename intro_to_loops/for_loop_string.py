name = input("Enter your name: ")

# preferable
for char in name:
    print(char)

# Manually getting the length of the name
# length = len(name)

# For each index, grab the character in the name string ['m', 'i', 'c', 'h', 'a', 'e', 'l']
# for i in range(length):
#     print(name[i])
